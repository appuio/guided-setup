name: Build

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

jobs:
  go:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Run build
        run: make build
